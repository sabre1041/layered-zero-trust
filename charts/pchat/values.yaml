# Default values for pchat

# PChat application configuration
app:
  name: pchat

# PostgreSQL database configuration
postgresql:
  name: pchat-db

  # Database configuration
  auth:
    database: pchat
    username: pchat_user
    # This is intentionally included here as an antipattern to be addressed
    password: RedH@tPassw0rd
  

  

