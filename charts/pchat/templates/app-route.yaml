apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: pchat
  name: pchat
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: pchat
    weight: 100
  wildcardPolicy: None