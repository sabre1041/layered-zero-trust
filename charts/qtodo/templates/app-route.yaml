apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: qtodo
  name: qtodo
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: qtodo
    weight: 100
  wildcardPolicy: None